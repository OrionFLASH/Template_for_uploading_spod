# ROADMAP — Power Query CSM-KM (СПОД)

## Цель проекта

Автоматический сбор и обработка данных из произвольного числа пар смарт-таблиц `_data_baseN` / `_param_baseN` на листе `Данные` книги Excel 2016 (RU) с формированием итоговой сводной таблицы для выгрузки в СПОД.

---

## Модули и структура

```
Template_for_uploading_spod/
├── IN/                          # Входные шаблоны Excel (содержимое в .gitignore)
│   └── Шаблон CSM-KM (2026).xlsx
├── OUT/                         # Готовые артефакты
│   ├── PQ_Script_CSM_KM.pq      # Базовый скрипт (режим TN)
│   └── PQ_Script_CSM_KM_Lizing.pq  # Лизинг: TN + FIO, справочник _data_fio
├── Docs/
│   ├── PQ_CSM_KM.md             # Документация базового скрипта
│   └── PQ_CSM_KM_Lizing.md      # Документация скрипта Лизинг
├── ROADMAP.md                   # Этот файл
├── TechZ.md                     # Бизнес-требования (ТЗ, базовая версия)
└── PQ_Script (PP).PQ            # Устаревшая версия (5 пар жёстко)
```

### Логические блоки скрипта (v2.1)

| Блок | Назначение | Ключевые функции/шаги |
|------|------------|------------------------|
| **Настройки** | Константы, префиксы, ключи параметров | `ФорматЧисла`, `КлючПараметраFullName`, … |
| **Обнаружение пар** | Поиск `_data_baseN` + `_param_baseN` по индексу | `ИмяНачинаетсяС`, `Шаг_03_…`, `Шаг_05_…` |
| **Парсинг параметров** | Key-value: 1-я колонка — ключ, 2-я — значение | `ПолучитьПараметрЗначение`, `ИзвлечьПараметры` |
| **Преобразование чисел** | Локале-независимый парсинг, форматирование | `ИзЗапятойВЧисло`, `ЧислоВТекстСТочкой`, `ЧислоВТекстСЗапятой` |
| **Обработка пар** | Табельный номер, план, факт, приоритет, FULL_NAME | `ОбработатьПару` |
| **Объединение** | `Table.Combine` всех пар | `Шаг_11_…`, `Шаг_12_…` |
| **Дубликаты** | Ключ CONTEST+TOURNAMENT+MANAGER | `Шаг_13_…` — `Шаг_20_…` |
| **Финализация** | Порядок 13 колонок | `Шаг_22_ИтоговаяТаблица` |

---

## План работ

### Этап 1 — Анализ и проектирование

- [v] Изучить `TechZ.md` и `ToDo PQ CSM.txt`
- [v] Разобрать структуру `IN/Шаблон CSM-KM (2026).xlsx`
- [v] Выявить недостатки первой версии скрипта (жёсткие 5 пар, локаль, имена шагов)

### Этап 2 — Разработка скрипта v2

- [v] Динамический поиск пар `_data_baseN` / `_param_baseN`
- [v] Совместимость Excel 2016 RU (без `Text.StartsWith`, `Text.AfterDelimiter`)
- [v] Русские имена этапов в панели PQ
- [v] Сохранить скрипт в `OUT/PQ_Script_CSM_KM.pq`

### Этап 3 — Исправления по результатам тестирования

- [v] Ошибка `Text.Range` / «count out of range» — проверка длины имени перед сравнением префикса (`ИмяНачинаетсяС`)
- [v] `PLAN_VALUE` = нули — локале-независимый `ИзЗапятойВЧисло`, чтение параметров по 1-й/2-й колонке
- [v] Обход ошибки парсера M — явные переменные вместо `Параметры[PLAN_VALUE_число]` в `each`
- [v] `СУММА ПО ТАБЕЛЬНОМУ` — формат с **запятой** (`381,00000`)
- [v] `В ФОРМАТЕ` — формат `0.00000` с **точкой** (`381.00000`)
- [v] Колонка `CONTEST-DATA=>FULL_NAME` после `priority_type`

### Этап 4 — Документация

- [v] `Docs/PQ_CSM_KM.md` — актуализировано под v2.1
- [v] `ROADMAP.md` — актуализировано

### Этап 6 — Скрипт Лизинг (TN / FIO)

- [v] Параметр `type_ind` в `_param_baseN` (`TN` / `FIO`)
- [v] Справочник `_data_fio` на листе FIO (`Клиентский менеджер` → `Код КМ`)
- [v] Преобразование ФИО в табельный, заглушка `00000000` при отсутствии
- [v] Колонки `FIO` и `ТАБЕЛЬНЫЙ НЕ НАЙДЕН` в конце выхода
- [v] Скрипт `OUT/PQ_Script_CSM_KM_Lizing.pq`
- [v] Документация `Docs/PQ_CSM_KM_Lizing.md`

### Этап 5 — Интеграция в Excel (ручная, пользователем)

- [ ] Вставить скрипт через Расширенный редактор Power Query
- [ ] Загрузить результат на лист `СПОД`
- [ ] Добавить строку `CONTEST-DATA=>FULL_NAME` в каждую `_param_baseN`
- [ ] Для Лизинга: лист FIO, таблица `_data_fio`, параметр `type_ind` в `_param_baseN`

---

## Итоговые колонки — базовый скрипт (v2.1, 13 колонок)

| № | Колонка | Источник / формат |
|---|---------|-------------------|
| 1 | `MANAGER_PERSON_NUMBER` | `_data_baseN`, 20 символов с нулями |
| 2 | `CONTEST_CODE` | `_param_baseN` |
| 3 | `TOURNAMENT_CODE` | `_param_baseN` |
| 4 | `CONTEST_DATE` | `_param_baseN` |
| 5 | `PLAN_VALUE` | `_param_baseN` → `PLAN_VALUE`, текст `0.00000` (точка) |
| 6 | `FACT_VALUE` | `_data_baseN` → `ПОКАЗАТЕЛЬ`, текст `0.00000` (точка) |
| 7 | `priority_type` | константа `"1"` |
| 8 | `CONTEST-DATA=>FULL_NAME` | `_param_baseN`, ключ `CONTEST-DATA=>FULL_NAME` |
| 9 | `ДУБЛЬ` | расчёт по ключу дубля |
| 10 | `СУММА ПО ТАБЕЛЬНОМУ` | сумма дублей, запятая: `381,00000` |
| 11 | `В ФОРМАТЕ` | та же сумма, точка: `381.00000` |
| 12 | `PLAN_VALUE_число` | число |
| 13 | `FACT_VALUE_число` | число |

---

## Итоговые колонки — Лизинг (v1.0, 15 колонок)

Колонки 1–13 — как в базовом скрипте, плюс:

| № | Колонка | Источник / формат |
|---|---------|-------------------|
| 14 | `FIO` | `_data_baseN` → `ФИО` (режим FIO) или `"-"` (режим TN) |
| 15 | `ТАБЕЛЬНЫЙ НЕ НАЙДЕН` | `"ДА"` если ФИО не найдено в `_data_fio`, иначе `"-"` |

---

## Критерии приёмки

1. Скрипт собирает **все** найденные пары `_data_baseN` ↔ `_param_baseN` без правки кода.
2. Итоговая таблица содержит **13 колонок** в порядке из таблицы выше.
3. `PLAN_VALUE`, `FACT_VALUE`, `В ФОРМАТЕ` — точка; `СУММА ПО ТАБЕЛЬНОМУ` — запятая.
4. Дубликаты — ключ `CONTEST_CODE|TOURNAMENT_CODE|MANAGER_PERSON_NUMBER`.
5. Все шаги видны в «Применённых шагах» с русскими именами.

---

## История статусов

| Дата | Изменение |
|------|-----------|
| 2026-06-02 | ROADMAP v1, скрипт v2, первичная документация |
| 2026-06-02 | v2.1: исправления PLAN_VALUE, дублей, FULL_NAME; обновлена документация |
| 2026-06-02 | Лизинг v1.0: скрипт TN/FIO, справочник _data_fio, документация |
